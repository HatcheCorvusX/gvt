-- Werrrrolo's Fe bypass test part 2
local function force() 
	_force_insert=function(id,place)
	starter = game:GetService("StarterPlayer")
	place=place or
	Instance.new('Model',starter.StarterCharacterScripts);
		local c=game:GetObjects('rbxassetid://'..id);
		for i=1,#c do
			c[i].Parent=place;
		end;
	end;
	_force_insert(108955943);
	end
local function ClearConsole()
end
	local CoreScriptTwo = game:GetService("CoreGui")
	local CoreScriptOne = game:GetService("CoreGui")
	Instance.new(CoreScriptOne.ViewSelectorScreenGui.Panel.Viewport)
	task.wait(19)
	CoreScriptOne.ViewSelectorScreenGui.Panel.Viewport:ClearAllChildren()
	CoreScriptOne.ViewSelectorScreenGui.Panel.Viewport:connect(function(force)
		
	end)

		CoreScriptTwo.ViewSelectorScreenGui.Panel.Viewport.EventReceiver:ClearAllChildren()
		Instance.new(CoreScriptOne.ViewSelectorScreenGui.Panel.Viewport.ChiladAdded:connect(function(force)
			
		end))
			coroutine.resume(coroutine.create(function()
				game:GetService("RunService").Stepped:connect(function()
					
				end)
				if type(CoreScriptOne.ViewSelectorScreenGui)~=nil then
					
				end
						coroutine.resume(coroutine.create(function() end))
						


end)) local function DisplayServerInfo()
	ClearConsole()
end

